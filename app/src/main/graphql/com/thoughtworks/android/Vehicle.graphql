subscription Vehicle($id: ID!) {
    getVehicleUpdate(id: $id)
    {
      id,
      type,
      brandName
    }
}

query VehicleEZ($id: ID!) {
  vehicle(id: $id)
  {
    id,
    type,
    modelCode
	}
}